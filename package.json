{
  "name": "token-factory-contracts",
  "version": "1.0.0",
  "description": "Smart contract repository for Token Factory - ERC20 token creation platform with advanced features across multiple blockchain networks",
  "private": true,
  "main": "contracts/src/TokenFactory.sol",
  "scripts": {
    "build": "cd contracts && forge build",
    "test": "cd contracts && forge test",
    "test:coverage": "cd contracts && forge coverage",
    "test:gas": "cd contracts && forge test --gas-report",
    "test:watch": "cd contracts && forge test --watch",
    "clean": "cd contracts && forge clean",
    "fmt": "cd contracts && forge fmt",
    "lint": "cd contracts && forge fmt --check",
    "lint:fix": "cd contracts && forge fmt",
    "node": "anvil",
    "deploy:local": "cd contracts && forge script script/Deploy.s.sol --broadcast --rpc-url anvil",
    "deploy:sepolia": "cd contracts && forge script script/Deploy.s.sol --broadcast --rpc-url sepolia --verify",
    "deploy:mainnet": "cd contracts && forge script script/Deploy.s.sol --broadcast --rpc-url mainnet --verify",
    "deploy:bsc-testnet": "cd contracts && forge script script/Deploy.s.sol --broadcast --rpc-url bsc_testnet --verify",
    "deploy:xsc": "cd contracts && forge script script/Deploy.s.sol --broadcast --rpc-url xsc --verify",
    "verify": "cd contracts && forge verify-contract",
    "inspect": "cd contracts && forge inspect",
    "doc": "cd contracts && forge doc",
    "slither": "cd contracts && slither .",
    "analyze:gas": "cd contracts && ./scripts/gas-benchmark.sh",
    "prepare": "husky install || true"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/token-factory.git"
  },
  "keywords": [
    "solidity",
    "smart-contracts",
    "ethereum",
    "bsc",
    "xsc",
    "erc20",
    "token-factory",
    "foundry",
    "blockchain",
    "defi",
    "web3"
  ],
  "author": "Token Factory Team",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/your-org/token-factory/issues"
  },
  "homepage": "https://github.com/your-org/token-factory#readme",
  "devDependencies": {
    "husky": "^8.0.3",
    "@commitlint/cli": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}